---
title: "Agriculture processing"
format:
  html:
    code-fold: true
jupyter: python3
---

```{python}
import os
os.chdir("..")
```

```{python}
from src.jp_imports.data_process import DataTrade
import polars as pl
import ibis
import pandas as pd
ibis.options.interactive = True
```

```{python}
d = DataTrade(debug=True)
```


```{python}
time = 2020
types = "naics"
df = d.conn.table("jptradedata")
start = ""
df = df.filter(
                (df.date >= start)
                & (df.date <= end)
)
df.filter(df.date.year() == 2009)
```

```{python}
url = f"{API_URL}/data/index/jp_index?update=false"
    df_monthly = pd.DataFrame(requests.get(url).json())

```